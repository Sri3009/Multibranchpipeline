pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'hostname -I'
                sh 'echo "task accomplished"'
                
            }
        }
    }
}
